<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} — errormine</title>
</head>
<body>
    {% include 'header.njk' %}
    <main>
        <section class="content">
            <h1>{{ title }}</h1>
            {{ content | safe }}
        </section>
    </main>
    {% include 'footer.njk' %}
</body>
</html>